<!DOCTYPE html>
<html>
  <head>
    <title>Catalago de Novels</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-1">
    <div class="container">
      <a class="navbar-brand" href="/">True Immortal Catalog</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse border" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item ">
            <a class="nav-link" href="/ideas">Novels |</a>
          </li>
          <li class="nav-item">
            <%= link_to 'Nova Novel |', new_idea_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="pages/info">Info</a>
          </li>
        </ul>
        <p class="navbar-text float-right"></p>
        <% if user_signed_in? %>
          Logado como: <strong><%= current_user.email %></strong>
          <%= link_to 'Editar perfil', edit_user_registration_path,  :class => 'navbar-link text-white nav-link'  %>|
          <%= link_to "Logout", destroy_user_session_path, method: :delete,  :class => 'navbar-link text-white nav-link'  %>
          <%= image_tag current_user.gravatar_url, :class => "gravatar border" %>
        <% else %>
          <%= link_to "Registrar-se", new_user_registration_path,  :class => 'navbar-link text-white nav-link'  %>|
          <%= link_to "Login", new_user_session_path, :class => 'navbar-link text-white nav-link'  %>
        <% end %>
      </div>
    </div>
  </nav>
  <div class="containter">
    <% if notice %>
      <p class=""alert alert-success><% notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <%= yield %>
  </div>
  <footer>
    <div class="container">
      Rails Girls <%= Time.now.year %>
    </div>
  </footer>
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
  </body>
</html>
